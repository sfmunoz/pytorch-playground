[project]
name = "pytorch-playground"
version = "0.1.0"
description = "PyTorch playground"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "torch>=2.12.0",
    "torchvision>=0.27.0",
]
